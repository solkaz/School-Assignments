#ifndef TYPES_HPP_
#define TYPES_HPP_

#include <string>
#include <vector>

using Word = std::string;
using WordPair = std::pair<Word, Word>;
using ParityBits = std::vector<int>;

#endif // TYPES_HPP_
