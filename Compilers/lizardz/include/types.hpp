#ifndef TYPES_HPP_
#define TYPES_HPP_

#include <string>
#include <vector>

using InOutPair = std::pair<int, int>;
using PillarMapCol = std::vector<int>;
using PillarMap = std::vector<PillarMapCol>;
using LizardMap = std::vector<std::string>;

#endif // TYPES_HPP_
