#include "tokenizer.hpp"

std::vector<std::string> Tokenize(std::string &source_code) {
    std::vector<std::string> tokens;

    auto source_code_begin = source_code.cbegin();
    auto source_code_end = source_code.cend();

    
}

bool IsKeyword(std::string &token) {
    
}
