#include "board.hpp"

#include <iostream>

int main() {
    unsigned int board_size,
	num_of_snakes;
    std::cin >> board_size >> num_of_snakes;

    Board play_board(board_size);
    
    
    return 0;
}
